1.  To install the requirements execute the below command on your terminal:
    pip install -r requirements.txt
  
2.  To generate the requirements.txt file after you have downloaded some packages execute the below command on your terminal:
    pip freeze > requirements.txt